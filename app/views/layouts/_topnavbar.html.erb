<div class="row border-bottom">
	<nav class="navbar navbar-static-top" role="navigation" style="margin-bottom: 0">
		<% if current_usuario.present? %>
      <div class="navbar-header">
          <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>
      </div>
		<ul class="nav navbar-top-links navbar-right">
			<li class="dropdown">
				<%= link_to ("#{image_tag(current_usuario.foto.url(:avatar)) if current_usuario.foto.present?}#{current_usuario.nome} <b class=\"caret\"></b>").html_safe, '#', data:{ toggle: 'dropdown' }, class: 'dropdown-toggle' %>
				<ul class="dropdown-menu animated fadeIn m-t-xs">
					<li><%= link_to "Dados Pessoais", "/crud/usuario/#{current_usuario.id}/edit" %></li>
					<li><%= link_to "Alteração de Senha", edit_usuario_path(current_usuario)%></li>
					<li class="divider"></li>
					<li><%= link_to "Sair", destroy_usuario_session_path, data: {method: :delete} %></li>
				</ul>
			</li>
		</ul>
		<% end %>
	</nav>
</div>
